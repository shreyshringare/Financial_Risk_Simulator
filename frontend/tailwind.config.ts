// Tailwind v4 — theme configured via @theme in globals.css
// This file is a no-op stub kept for tooling compatibility.
import type { Config } from "tailwindcss";
const config: Config = {};
export default config;
